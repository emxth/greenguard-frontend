import { Box, Typography } from "@mui/material";

function PickupRequests() {
    return (
        <Box>
            <Typography variant="h4" sx={{ textAlign: 'center' }}>My Pickup Requests</Typography>
        </Box>
    );
}

export default PickupRequests;